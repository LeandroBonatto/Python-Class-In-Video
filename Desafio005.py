# n = int(input('Escreva algum valor: '))
# a = n - 1
# s = n + 1
# print('Considerando o valor {}, seu antecessor sera {:.2f} e o sucessor sera {:.2f}'.format(n, a, s))

n = int(input('Enter some value: '))
pred = n - 1
suc = n + 1
print('Considering the value of {}, its predecessor will be {:.2f} and the successor will be {:.2f}'.format(n, pred, suc))